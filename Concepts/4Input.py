input() # = Funtion (method) that prompts the user to enter data
        #   Returns the entered data as a string
        
name = input()

#It is good to format the input so the interpreter knows what we want it to be
number = input(int())
price = input(float())
name = input(str())